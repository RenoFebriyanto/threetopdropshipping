<footer class="bg-[#111111] text-[#a0a0a0] pt-20 pb-10 px-margin-mobile md:px-margin-desktop font-body-md mt-auto">
  <div class="max-w-container-max mx-auto grid grid-cols-1 md:grid-cols-4 gap-12 md:gap-8 mb-16">

    <!-- Brand Column -->
    <div class="flex flex-col gap-6">
      <a href="{{ routes.root_url }}" class="font-headline-lg text-white text-[28px] uppercase tracking-widest">
        {{ shop.name }}
      </a>
      <p class="text-sm leading-relaxed max-w-[280px]">{{ section.settings.tagline }}</p>
    </div>

    <!-- Shop Column -->
    <div class="flex flex-col gap-6">
      <h3 class="text-white font-label-sm text-label-sm uppercase tracking-widest">Shop</h3>
      <ul class="flex flex-col gap-4 text-sm">
        {% if section.settings.footer_menu != blank %}
          {% for link in linklists[section.settings.footer_menu].links %}
            <li><a href="{{ link.url }}" class="hover:text-white transition-colors duration-300">{{ link.title }}</a></li>
          {% endfor %}
        {% else %}
          <li><a href="/collections/new-arrivals" class="hover:text-white transition-colors duration-300">New Arrivals</a></li>
          <li><a href="/collections/all" class="hover:text-white transition-colors duration-300">Our Collection</a></li>
          <li><a href="/search" class="hover:text-white transition-colors duration-300">Search</a></li>
        {% endif %}
      </ul>
    </div>

    <!-- Company Column -->
    <div class="flex flex-col gap-6">
      <h3 class="text-white font-label-sm text-label-sm uppercase tracking-widest">Company</h3>
      <ul class="flex flex-col gap-4 text-sm">
        <li><a href="/pages/our-story" class="hover:text-white transition-colors duration-300">Our Story</a></li>
        <li><a href="/pages/sustainability" class="hover:text-white transition-colors duration-300">Sustainability</a></li>
        <li><a href="/pages/contact" class="hover:text-white transition-colors duration-300">Contact</a></li>
      </ul>
    </div>

    <!-- Newsletter Column -->
    <div class="flex flex-col gap-6">
      <h3 class="text-white font-label-sm text-label-sm uppercase tracking-widest">Join the Club</h3>
      <p class="text-sm">Subscribe to receive updates and access to exclusive deals.</p>
      {% form 'customer', id: 'footer-newsletter' %}
        <div class="flex items-center border-b border-[#a0a0a0] py-2 mt-2">
          <input type="hidden" name="contact[tags]" value="newsletter">
          <input class="bg-transparent border-none outline-none w-full text-white placeholder-[#a0a0a0] text-sm" placeholder="Email Address" type="email" name="contact[email]" required autocomplete="email">
          <button type="submit" class="text-white hover:text-[#a0a0a0] transition-colors duration-300 flex-shrink-0" aria-label="Subscribe">
            <span class="material-symbols-outlined" style="font-size:20px">arrow_forward</span>
          </button>
        </div>
      {% endform %}
    </div>
  </div>

  <!-- Bottom Bar -->
  <div class="max-w-container-max mx-auto pt-8 border-t border-[#222222] flex flex-col md:flex-row justify-between items-center gap-4 text-xs font-label-sm text-label-sm tracking-wider">
    <p>&copy; {{ 'now' | date: '%Y' }} {{ shop.name }}. All rights reserved.</p>
    <div class="flex gap-6">
      {%- comment -%} Shipping Policy link: prefer Pages handle, fallback to shop policy {%- endcomment -%}
      {%- if pages['shipping-policy'] -%}
        <a href="{{ pages['shipping-policy'].url }}" class="hover:text-white transition-colors duration-300">{{ pages['shipping-policy'].title | default: shop.shipping_policy.title }}</a>
      {%- elsif shop.shipping_policy -%}
        <a href="{{ shop.shipping_policy.url }}" class="hover:text-white transition-colors duration-300">{{ shop.shipping_policy.title }}</a>
      {%- endif -%}

      {%- comment -%} Refund Policy link: prefer Pages handle, fallback to shop policy {%- endcomment -%}
      {%- if pages['return-policy'] -%}
        <a href="{{ pages['return-policy'].url }}" class="hover:text-white transition-colors duration-300">{{ pages['return-policy'].title | default: shop.refund_policy.title }}</a>
      {%- elsif shop.refund_policy -%}
        <a href="{{ shop.refund_policy.url }}" class="hover:text-white transition-colors duration-300">{{ shop.refund_policy.title }}</a>
      {%- endif -%}

      {%- comment -%} Privacy Policy link: prefer Pages handle, fallback to shop policy {%- endcomment -%}
      {%- if pages['privacy-policy'] -%}
        <a href="{{ pages['privacy-policy'].url }}" class="hover:text-white transition-colors duration-300">{{ pages['privacy-policy'].title | default: shop.privacy_policy.title }}</a>
      {%- elsif shop.privacy_policy -%}
        <a href="{{ shop.privacy_policy.url }}" class="hover:text-white transition-colors duration-300">{{ shop.privacy_policy.title }}</a>
      {%- endif -%}

      {%- comment -%} Terms of Service link: check common page handles, then fallback to shop terms {%- endcomment -%}
      {%- if pages['terms-of-use'] -%}
        <a href="{{ pages['terms-of-use'].url }}" class="hover:text-white transition-colors duration-300">{{ pages['terms-of-use'].title | default: shop.terms_of_service.title }}</a>
      {%- elsif pages['terms-of-service'] -%}
        <a href="{{ pages['terms-of-service'].url }}" class="hover:text-white transition-colors duration-300">{{ pages['terms-of-service'].title | default: shop.terms_of_service.title }}</a>
      {%- elsif shop.terms_of_service -%}
        <a href="{{ shop.terms_of_service.url }}" class="hover:text-white transition-colors duration-300">{{ shop.terms_of_service.title }}</a>
      {%- endif -%}
    </div>
  </div>
</footer>

{% schema %}
{
  "name": "Footer",
  "settings": [
    {
      "type": "text",
      "id": "tagline",
      "label": "Brand Tagline",
      "default": "Redefining modern luxury through sustainable craftsmanship and timeless design."
    },
    {
      "type": "link_list",
      "id": "footer_menu",
      "label": "Footer Menu",
      "default": "footer"
    }
  ]
}
{% endschema %}
